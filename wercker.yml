box: ruby
build:
  steps:
    - script:
      name: fix nokogiri
      code: bundle config build.nokogiri --use-system-libraries
    - bundle-install
    - script:
        name: rspec
        code: bundle exec rspec
